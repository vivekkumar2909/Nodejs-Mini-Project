<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit Post</title>
    <script src="https://cdn.jsdelivr.net/npm/@tailwindcss/browser@4"></script>
</head>

<body class="min-h-screen bg-gradient-to-br from-black via-zinc-900 to-zinc-800 
             flex flex-col items-center justify-center text-white py-10 px-4">

    <div class="w-full max-w-md bg-white/5 backdrop-blur-xl 
                border border-white/10 rounded-3xl shadow-2xl p-8">

        <h2 class="text-2xl font-semibold text-center mb-6 text-indigo-400">
            ✏ Edit Your Post
        </h2>

        <div class="absolute top-4 right-4">
            <a href="/profile/" class="text-sm px-3 py-1 rounded-full bg-red-500/20 
                              hover:bg-red-500/40 text-red-400 transition duration-300">
                Cancel
            </a>
        </div>

        <form action="/edit/<%= post._id %>" method="POST" class="space-y-5">

            <textarea name="content" rows="4" required class="w-full bg-zinc-800/60 border border-zinc-700 
                      rounded-xl p-4 focus:outline-none 
                      focus:ring-2 focus:ring-indigo-500 
                      resize-none transition duration-300"><%= post.content %></textarea>

            <button type="submit" class="w-full bg-indigo-600 hover:bg-indigo-700 
                    py-3 rounded-xl font-medium 
                    transition duration-300 shadow-lg 
                    hover:shadow-indigo-500/40">
                Update Post 🚀
            </button>

        </form>
    </div>

</body>

</html>